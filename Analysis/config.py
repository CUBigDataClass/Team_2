consumer_key = 'Going to push basic config then add it to git ignore'# remove this before git push
consumer_secret = '' # remove this before git push
access_token = '' # remove this before git push
access_token_secret = '' # remove this before git push
